Windows10 VM with Vagrant and Ansible
-------------------------------------

### Setup

```
$ brew tap caskroom/cask
$ brew install brew-cask
$ brew cask install virtualbox
$ brew cask install vagrant
```
